# 真机联调纪要（2026-09-13 夜）

设备：OnePlus 13 `3B159H003D600000` · GKD 1.12.1 · 拼多多 8.24.0 · 屏幕 1440×3168

## 结果摘要

| 步骤 | 结果 | 证据 |
|---|---|---|
| GKD 冲突规避 | **完成** | MarsGao 七组规则开关全部关闭；奥怪仍启用（广告） |
| 订阅版本 | 手机仍为 **v5**；仓库已推送 **v6**（`18a91d6`） | 需手机点更新或重导入 |
| 双重补贴领取 | **verified** | `去使用 0→2`，券包可见「百亿补贴频道-百亿消费券活动获得」 |
| 进消费券会场 | **跑通** | 路径见下；`brand_rebate` 失败；裸 `coupons.html` 常进「我的优惠券」 |
| 地区专享 Tab | **verified** | 节点点击 `(972,782)`；本屏无「立即领取」→ `unavailable`（可能已领完/无新券） |
| 点亮 | 部分 | UI 出现「解锁点亮」与 1/6 星；WebView 节点不稳定 |
| 会员打卡 | 未闭环 | 曾进会员页见「打卡」；uiautomator 在动画页长期 `idle state` 失败打断 |

## 稳定进场路径（已写入 `enter_coupon_venue.py --open-subsidy`）

1. `pinduoduo://.../search_result.html?search_key=%E7%99%BE%E4%BA%BF%E8%A1%A5%E8%B4%B4`
2. 等待百亿补贴频道加载
3. 点击右卡「百亿消费券」**标题区** `tap(1000, 980)`（抽福袋约在 y≈1065，勿点）

辅助：关闭动画 scale=0 可显著提高 `uiautomator dump` 成功率。

## 脚本模式注意

- 中文 `input text` **不可用**；搜索请用 URL 编码深链。
- 会场在 WebView 内，倒计时动画会导致 dump 失败 → 先 `settings put global *_animation_scale 0`。
- 勿点「抽福袋 / 去使用 / 去拼单」。

## 未完成 / 次日

1. 手机导入订阅 **v6**，按矩阵只开 key1–3（及验收后的 key5）。
2. 会员打卡 + 等级礼包在动画关闭后重跑。
3. 地区专享在有「立即领取」的业务日再验 `verified` 分支。
4. P1-C：GKD 单独触发 vs 脚本基线对比（规则保持关闭期间只做脚本）。
